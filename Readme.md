## ubuntu

  *  ***1. Installtion***
      * Get Ubuntu 20.04
      * install ubuntu 20,04

   *  **2.  Initial Settings :**
      * adduser account
      * enable root user
      * network settings
      * service settings
      * update system 
      * configure vim 
      * configure sudo

## Ntp and Ssh server

   * _**1. ntp service**_
      * configure ntp server (NTPD)
      * configure ntp server (chrony)
      * server client 
   * _**2. ssh service**_   
      * ssh service configuration
      * Password Authentication
      * SSH file transfer(ubuntu)
      * SSH file transfer (Linux)
      * SSH Key-Pair Authentication
      * SFTP only + Chroot
      * use SSHPpass 
      * use SSH-AGENT
      * use SSHFS
      * ssh port forwarding 
      * use parallel ssh
       
## dns /dhcp server
      
      * install dnsmanq 
      * configure DHCP server

   * _**DNS server(Bind)**
      * configure for internal network
      * configure for external network
      * configure zone files 
      * verity resolution 
      * use view statement
      * set alias (CNAME)
      * configure secondary server
    **Dhcp server**
      * configure dhcp server
      * configure dhcp client

## Storage server 
    
   ### NFS
      * configure nfs server
      * configure nfs client
      * NFS 4 ACL tool
   ### iSCSI
    
      * conf iscsi target(targetcli)
      * configure iscsi target (tgt)
      * configure iscsi initiator 

   ### GlusterFS

      * install Glasterfs
      * disdributed configuration
      * GlusterFS Client
      * GlusterFS + NFS-Ganesha
      * Add Nodes (Bricks)
      * Remove Nodes (Bricks)
      * Replication Configuration
      * Distribution + replication
      * dispersed configure
    
   ### Ceph Octopus

      * configure cluster 1
      * configure cluster 2
      * use block ds
      * use file system 
      * ceph object gateway
      * enable dashboard
      * add or remove osds
      * cephFS + NFS-Ganesha
    
   ### Next cloud
    
      * install 
      * Add user accounts
      * upload files
      * Accses webdav
      * Access via desktop client 
      * Chat / Talk / Screen Share
      * use mail 
      * use calander
      * use address book 
      * use external storage 
      * LDAP Integration

##  Virtualization
   
   ### Kvm
    
      * install kvm 
      * create the virtul machine
      * create virtual machine (GUI)
      * Basic operation 
      * Management tools
      * Spice server 
      * spice client 
      * nested KVM 
      * live migration
      * storage migrtion

   ### virtual box
      
      * Install vitual box
      * crete thevirtual machine 
      * install guest addition 

##  Container Platform
   
   ### Docker
      
      * install docker 
      * add container image
      * Access contsiner service
      * use docker file
      * use external storage 
      * use external storage(NFS)
      * use docker compose 
      * use registry
      * docker netork basic 
      * docker swarm cluster
   
   ### Microk8

      * install microk8
      * deploy pods 
      * add nodes
      * enable dashboard 
      * enable storage 
      * enable resitry
      * enable fluentd
      * enable Promethus
   
   ### Minikubs
      * install minikubs
      * install minikubs (host)
      * Basic usage
   
   ***kubernets***

      * install kubeadm
      * configure master node
      * configure worked node
      * deploy pod
      * confogure presistent storage
      * configure private resitry
      * add nodes
      * remove NODES
      * DEPLOY metrics server 
      * horizantal pod autscaler
      * enable dashboard
      * instsll helm
      * Dynamic provicenig 
      * Deploy promethes

## cloud compute

   ***Open stock yoga*** 
   - Yoga over view 
   - pre - requirment
   - configure key-stone #1
   - configure key-stone #2
   - configure Glance
   - Add VM image
   - configure Nova #1 
   - configure nova #2
   - configure nova #3
   - configure neutron #1
   - configure neutron #2
   - configure networking 
   - Add user users 
   - create instance
   - configure horzon
   - Add compute nodes
   - conf neutron (control node)
   - conf neutron (network node)
   - conf neurtron (computer node)
   - Neutron network (FLAT)
   - conf neutron (VXLAN)
   - neutron network (VXLAN)
   - conf neutron Ovn (network)
   - neutron network ovn (computer)
   - conf clinder (control node)
   - conf cinder (storage node)
   - cinder storage(LVM)
   - cinder storage (NFs)
   - cinder storage (MULTI-BACK) 
   - cinder backup service 
   - configure heat
   - configure heat(contol node)
   - configure heat(network node)
   - how to use heat 
   - configure barbican
   - how to use barbican
   - configure swift(control node)
   - configure swift(storage node)
   - configgure swift(proxy node)
   - how to use switf
   - conf manila (centrol node)
   - configure desinate(local device)
   - conf desinate (netork node)
   - how to use desinate
   - conf octovia (control node)
   - conf octvia  (network node)
   - conf magnum(control node)
   - create load blancer image
   - how to use magnam
   - configure Gnoccchi 
   - conf celometer (computer)
   - conf celiometer(network)
   - how to use celiometer (nova)
   - how to use cileometer(Glence)
   - how to use celiometer(cinder)

  ##  
    openstock xena
      openstock wallaby
        open stock victoria
         openstock Ussuri
    
    
    
    
      
    
    
    
| TOPIC NAME | DOCUMENTATION |
|---|---|
| samba configuration | samba-configuration-centos.md |
| ssh configuration | ssh-configuration.md |
| ftp configuration | vsftp-configuration.md |
